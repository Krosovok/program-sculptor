<UserControl x:Class="UI.Controls.OrdinalFieldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UI.Controls"
             xmlns:content="clr-namespace:UI.Content;assembly=UI.Content"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             DataContextChanged="DataContextChange">
    <UserControl.Resources>
        <content:ColorConverter x:Key="ColorConverter" />
    </UserControl.Resources>
    <Grid>
        
        <!-- TODO: Write field from the beginning. Use Canvas and Rectangle classes.
            Bind Rectangle color to element color, and it's location to X and Y.
        -->
        <ScrollViewer HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
            <Grid x:Name="FieldGrid">
                
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>